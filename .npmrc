# 이슈 #28. 배포된 지 24시간이 안 된 버전은 설치하지 않는다.
minimum-release-age=1440
# node_modules가 잠금 파일과 어긋나면 스크립트 실행을 거부한다.
verify-deps-before-run=error
# 허용 목록에 없는 패키지가 빌드 스크립트를 가지면 설치를 실패시킨다.
strict-dep-builds=true
