# 로컬 개발·관리용 .env.local 템플릿. 이 파일 전체를 Netlify에 가져오지 않는다.
# 운영 업로드 파일은 pnpm env:remote로 생성한다. docs/NETLIFY-ENV.md 참고.
# Update these with your Supabase details from your project settings > API
# https://app.supabase.com/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=your-project-url
NEXT_PUBLIC_SUPABASE_KEY=your-publishable-key

# 개발 전용 Better Auth / 카카오 검증. pnpm kakao:setup으로 .env.local을 준비한다.
# 이 기능은 NODE_ENV=production에서 항상 비활성화된다.
KAKAO_AUTH_TEST_ENABLED=false
KAKAO_CLIENT_ID=
KAKAO_CLIENT_SECRET=
KAKAO_APP_ID=
BETTER_AUTH_SECRET=
KAKAO_TEST_DB_PASSWORD=
KAKAO_TEST_DATABASE_URL=

# 새 better_auth 스키마 검증. pnpm auth-migration:setup으로 로컬 값을 생성한다.
AUTH_MIGRATION_DB_PASSWORD=
AUTH_MIGRATION_DATABASE_URL=
# 이전 전체 이관 경로를 선택할 때만 필요하다. 신규 시작에는 사용하지 않는다.
SUPABASE_MIGRATION_SOURCE_DATABASE_URL=

# 실제 서비스 연결은 검증한 배포 환경에서 별도로 설정한다.
AUTH_DATABASE_URL=
AUTH_PHONE_DATABASE_URL=
KAKAO_REMOTE_AUTH_TEST_ENABLED=false
BETTER_AUTH_URL=
# Supabase에 가져온 동일 ES256 개인 JWK(JSON, kid 포함). 서버 전용.
SUPABASE_DATA_SIGNING_JWK=
# 실제 Data API 서명 검증·새 정책 적용·인증 DB 일치를 확인한 후에만 true.
BETTER_AUTH_DATA_API_READY=false
